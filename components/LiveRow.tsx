import React from "react"; import Image from "next/image"; import "../styles/thumb.css"; export default function LiveRow(){ const thumbs=["/thumbnails/live1.jpg","/thumbnails/live2.jpg","/thumbnails/live3.jpg","/thumbnails/live4.jpg"]; return(<div className="thumb-row live-row">{thumbs.map((src,i)=>(<div key={i} className="relative"><Image src={src} alt={`Live ${i+1}`} width={320} height={180} className="rounded shadow-lg ring-2 ring-red-600 animate-pulse" /></div>))}</div>);} 
