\"use client\";import Image from "next/image";import Link from "next/link";type Props={href?:string;src?:string;alt?:string;title?:string;className?:string;[k:string]:any};export default function Thumb({href=\"#\",src,alt=\"thumb\",title,className=\"\",...rest}:Props){const content=(<div className={`thumb ${className}`} {...rest}>{src?<Image src={src} alt={alt} width={320} height={180} />:<div style={{width:320,height:180,background:\"#222\"}}/>}{title?<div className=\"thumb-title\">{title}</div>:null}</div>);return href?<Link href={href}>{content}</Link>:content;}
