export default function Creator(){ return <main style={{maxWidth:600,margin:"60px auto",padding:"0 20px",textAlign:"center"}}><h1>Creator</h1><p>Temporary placeholder. Replace later.</p></main>; }
export async function getServerSideProps(){ return { props: {} }; }

export const config = { runtime: 'experimental-edge' };
