export const dynamic ="force-dynamic";
export default function Stream(){ return <main style={{maxWidth:600,textAlign:"center"}}><h1>Stream</h1><p>Temporary placeholder. Replace later.</p></main>; }
export async function getServerSideProps(){ return { props: {} }; }

export const config = { runtime: 'experimental-edge' };
